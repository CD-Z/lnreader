// Deprecated local config: re-export the root config per Tamagui docs
export { default } from '../../tamagui.config';
export { config as tamaguiConfig } from '../../tamagui.config';
export type { OurConfig as Conf } from '../../tamagui.config';
// Kept for compatibility with existing imports
